{
  "installCommand": "npm install && gem install bundler && rm -f Gemfile.lock && bundle install",
  "buildCommand": "bundle exec jekyll build",
  "outputDirectory": "_site",
  "functions": {
    "api/checkout.js": {
      "includeFiles": "db/checkout-schema.sql",
      "maxDuration": 10
    }
  }
}
